// Smoke test for the VentExpenseApp.
//
// This verifies that the app launches and the HomeShell renders
// with the bottom navigation bar and initial screen.

import 'package:flutter_test/flutter_test.dart';

// Note: Full widget tests require DI setup with mocked repositories.
// This file serves as the entry point for widget-level smoke tests
// once the DI container supports test overrides.

void main() {
  test('placeholder — app smoke test requires DI mock setup', () {
    // The actual widget test will be implemented when we add
    // a test-friendly DI configuration.
    expect(true, isTrue);
  });
}
